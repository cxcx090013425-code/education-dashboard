# 生成后对话调整机制

> 设计目标：教案/练习生成后，Terra看完可以告诉Tellus哪里要改，Tellus改完重新生成，而不是每次从零开始。

---

## 当前问题

现在的流程是：
1. Tellus生成完整内容
2. 输出PDF/Word
3. Terra看完觉得要改
4. Tellus从头重新生成一遍

**问题**：每次改一个细节都要全量重生成，浪费token，而且改完可能把没毛病的部分也改坏了。

---

## 新流程设计

### 核心原则
- **增量修改**：只改Terra指定的部分，不动其他内容
- **保留历史**：每次修改留记录，知道改了什么、为什么改
- **最小输出**：修改后只输出变更部分，不重发整份文档

### 流程

```
第1步：Tellus 生成初稿
         ↓
第2步：输出到工作间（Markdown，不是PDF）
         ↓
第3步：Terra 阅读，标注要改的地方
         ↓
第4步：Tellus 读取原稿 + Terra的修改意见
         ↓
第5步：Tellus 只改指定部分，输出修改后的完整文档
         ↓
第6步：Terra 确认通过 → Calendula 固化成PDF/Word
```

### 修改意见格式（Terra用）

Terra告诉Tellus要改什么时，用这个格式：

```
改 B2 教案第3课时：
- 教学流程第2步太长，拆成两步
- 讨论问题换成更贴近生活的话题
- 时间分配：导入从5分钟改成3分钟

其他部分不动。
```

关键：**明确说"其他部分不动"**，Tellus就不会手贱去改不该改的。

### Tellus 收到修改意见后

1. 读取原稿
2. 只改Terra指定的部分
3. 输出时标注修改位置：`[已修改] 原文xxx → 新文xxx`
4. 确认没有误改其他部分

---

## 对Caelum的脚本需求

### 需要Caelum做的事

1. **生成脚本支持"读取已有文件+增量修改"**
   - 当前：脚本 = 从零生成完整文件
   - 改后：脚本 = 读取已有文件 → 接收修改指令 → 只改指定部分 → 输出新文件

2. **修改记录功能**
   - 每次修改自动记录：时间、改了什么、为什么改
   - 存在 `修改记录.md` 里，和原稿放同一个目录

3. **版本管理**
   - 每次修改后保存一个新版本：`教案_v1.md` → `教案_v2.md`
   - 不覆盖原文件，方便回溯

---

## 验证标准

- [ ] Terra说"改第3课时"，Tellus只改第3课时，其他课时一字不动
- [ ] 修改后输出的文档里能清楚看到哪里改了
- [ ] 改3次之后，仍然能回溯到第1版
- [ ] 整个过程比"从零重生成"省至少50%的token
